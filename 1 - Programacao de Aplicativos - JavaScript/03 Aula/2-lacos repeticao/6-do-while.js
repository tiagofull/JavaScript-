/**
DO WHILE
* dependendo da condição o laço WHILE não é executado, porque a condição
é verificada antes[
* DO WHILE faz com que a condição seja executada pelo menos uma vez,
 mesmo se acondição for falsa
        SINTAXE:
            do{
                bloco de código;
            }while(condicao);

] 
* 
 */

let x = 0;


do{
    document.write(`${x} <br>`);
    x++;
}while(x < 0);



